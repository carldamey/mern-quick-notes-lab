export default function NotesPage() {
	return <h1>No Notes Yet!</h1>
}

//import array of notes matching current user, ideally make notes an array in the user model so it doesn't need to be searched for every time